A simple command-line based To-Do List app written in Python. This app allows users to add tasks, view them, mark them as complete, and delete them. Task data is stored in a local JSON file.

📂 Features
Add new tasks
View all tasks
Mark tasks as done
Delete tasks
Persistent storage using tasks.json
